// require("dotenv").config();
// import dotenv from "dotenv";
// dotenv.config();

const config = {
  // REACT_APP_API_URL: process.env.REACT_APP_API_URL || "https://node.transfunnel.io:3001",
  // REACT_APP_BASE_PATH: process.env.REACT_APP_BASE_PATH || "/",
  REACT_APP_IMAGE_SRC: "/testbuild/build", 
};

export { config };
